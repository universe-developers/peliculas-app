import { CutoutTextPipe } from './cutout-text.pipe';

describe('CutoutTextPipe', () => {
  it('create an instance', () => {
    const pipe = new CutoutTextPipe();
    expect(pipe).toBeTruthy();
  });
});
